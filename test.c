#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <ctype.h>

void vFunc(int x) {
    int x = 5;
}


int main(int argc, char const *argv[]) {
    /* code */
    // for (int i = 10; i > 0; --i)
    // {
    //     printf("%i\n", i);
    // }
    // return 0;
    // int buffer[100];
    // int x = sizeof buffer;

    //int x = sizeof "moose";
    // int x = atoi(argv[1]);
    srand(time(NULL));
    printf("%i\n", rand());
}
